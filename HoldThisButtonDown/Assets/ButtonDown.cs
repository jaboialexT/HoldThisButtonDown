using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonDown : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        /*
         *  if(button is pressed)
         *  timer counts from 0
         *  when button is released
         *  timer stops
         *  time is saved
         *  time is sent to database
         */
    }
}
